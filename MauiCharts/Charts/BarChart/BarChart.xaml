<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiCharts.Charts.BarChart"
             xmlns:charts="clr-namespace:MauiCharts.Charts">
    <charts:BarChartGraphicsView x:Name="Chart"
                  HeightRequest="500"
            WidthRequest="200"
                                 Points="{Binding .}"/>
    <Slider MinimumTrackColor="Violet"
            MaximumTrackColor="#7F2CF6"
            Minimum="0"
            Maximum="1"
            WidthRequest="200"
            ThumbColor="Purple"
            Margin="30,0,30,0"
            HorizontalOptions="FillAndExpand"
            Value="{Binding Source={x:Reference Chart}, Path=XAxisScale}"/>
</StackLayout>